╔════════════════════════════════════════════════════════════════╗
║                  MOBILE APP SERVER - SETUP                     ║
╚════════════════════════════════════════════════════════════════╝

🎯 QUICK START (Choose One):

┌────────────────────────────────────────────────────────────────┐
│  Option 1: AUTO START (Recommended)                           │
│  ─────────────────────────────────────────────────────────     │
│  1. Double-click: start-server.bat                            │
│  2. Server starts with auto IP detection                      │
│  3. Done! ✓                                                   │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│  Option 2: MANUAL STEPS                                       │
│  ─────────────────────────────────────────────────────────     │
│  1. Run: setup-ip.bat                                         │
│  2. Run: node app.js                                          │
│  3. Done! ✓                                                   │
└────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════

📋 WHAT HAPPENS AUTOMATICALLY:

  ✓ Detects your PC's IP address (e.g., 192.168.111.58)
  ✓ Updates lib/config.dart with correct IP
  ✓ Configures server for your network
  ✓ Shows connection URL in console

═══════════════════════════════════════════════════════════════════

🔄 WHEN TO RUN AGAIN:

  • Changed WiFi network
  • IP address changed
  • Moving to different location
  • Connection issues with mobile app

═══════════════════════════════════════════════════════════════════

📱 CONNECTING YOUR APP:

  1. Start server (use start-server.bat)
  2. Note the IP shown in console
  3. Restart your Flutter app
  4. Both must be on SAME WiFi network!

═══════════════════════════════════════════════════════════════════

🛠️ TROUBLESHOOTING:

  Problem: App can't connect
  ─────────────────────────────
  → Check: Same WiFi network?
  → Check: Firewall allows port 3000?
  → Try: Run start-server.bat again
  → Try: Restart Flutter app

  Problem: Wrong IP detected
  ─────────────────────────────
  → Check: ipconfig (Windows CMD)
  → Look for: IPv4 Address
  → Edit: lib/config.dart manually if needed

═══════════════════════════════════════════════════════════════════

✅ FILES CREATED:

  start-server.bat    → One-click server start + IP update
  setup-ip.bat        → Update IP configuration only
  update-ip.js        → IP detection script
  README.md           → Detailed documentation

═══════════════════════════════════════════════════════════════════

💡 TIP: Add start-server.bat to your desktop for easy access!

═══════════════════════════════════════════════════════════════════
